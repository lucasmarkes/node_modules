## Módulos: conjuntos de código que conseguem ou não ser reutilizados


*3* tipos de módulos:
- Dentro do Node, todos os arquivos sao modulos;
- Nativos, já vem com o próprio Node (ex: http);
- Do npm (pacotes)
